# GPU-Accelerated PyTorch for NVIDIA GPUs on Windows
# Install with: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
# Or for CUDA 11.8: --index-url https://download.pytorch.org/whl/cu118
torch>=2.1.0
torchvision>=0.16.0
torchaudio>=2.1.0

# Transformers and VLM dependencies
transformers>=4.37.0
accelerate>=0.25.0  # For efficient multi-GPU and device mapping
qwen-vl-utils>=0.0.2
sentencepiece>=0.1.99  # For tokenization
protobuf>=3.20.0

# Image processing
pillow>=10.0.0
pillow-simd>=10.0.0  # SIMD-optimized PIL for faster image ops

# Numerical computing
numpy>=1.24.0

# Windows screen capture
mss>=9.0.0
pywin32>=306

# GPU optimization libraries
ninja>=1.11.0  # Required for flash-attention compilation
flash-attn>=2.5.0  # Flash Attention 2 for 2-4x faster inference (requires CUDA + Visual Studio)

# Optional: Quantization support for lower memory usage
bitsandbytes>=0.41.0  # 8-bit/4-bit quantization (Windows support may vary)

# Development dependencies
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-mock>=3.12.0
black>=23.0.0
flake8>=6.0.0
mypy>=1.0.0
types-Pillow>=10.0.0
